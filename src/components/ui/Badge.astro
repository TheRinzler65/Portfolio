---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class:list={[
    "font-mono text-xs bg-surface-overlay text-text-muted px-2.5 py-1 rounded-md border border-border",
    className,
  ]}
>
  <slot />
</span>
